# SonderKey 1.4.2

- **The handwriting plugin download button is visible again.** Its label used the colour for text on a normal key, which in most themes is near-white — the same as the action key background it was drawn on, so the button appeared as a blank bar. The label colour is now derived from the background it sits on, so it can never vanish, and the button is larger with a clearer shape.
