# SonderKey 1.4.1

- The microphone key is **pinned above the keyboard by default** rather than sitting in the toolbar row, so it is reachable without expanding the toolbar. Either placement can still be chosen from the Voice typing screen.
