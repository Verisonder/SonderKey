# SonderKey 1.3.1

- The microphone key now appears whenever SonderKey's own voice typing is set up. It was previously hidden unless the system had a separate voice input method installed, which made the toolbar key impossible to show on devices without one — including after enabling it in the toolbar settings.
