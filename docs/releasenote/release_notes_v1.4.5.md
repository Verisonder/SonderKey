# SonderKey 1.4.5

- **The main dictionary can now be installed during setup.** Gesture typing matches a swipe against dictionary entries, so without a dictionary the engine runs and returns nothing — with no indication of why. Setup previously offered the emoji dictionary and the gesture library but not the dictionary both of them depend on.
