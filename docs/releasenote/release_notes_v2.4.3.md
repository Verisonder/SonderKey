# SonderKey 2.4.3

- **Check for updates from inside the app.** About now has an entry that looks for a newer release on GitHub, downloads the build matching your variant, and hands it to the system installer. Nothing installs without your confirmation.
- The offline variants say plainly that this is unavailable, since they have no internet access by design.
